import styled from "styled-components";

export const CanvasPage = styled.html`
  margin: 0;
  padding: 0;
  box-sizing: border-box;
`;

export const BodyCanvas = styled.body`
  background: black;
`;

export const Canvas = styled.canvas`
  position: absolute;
  max-width: 100%;
`;
